#include "funciones.hpp"

int main(int argc, char const *argv[])
{
	//laplace(); // Resolvemos la ecuacion de laplace en dos dimensiones
	//laplaceS(); // Resolvelos la ecuacuin de laplace en dos dimensiones en un semicirculo
	poisson();  // Resolvemos la ecuacion de poisson
	//weird(); // Validamos el sistema SYSG
	return 0;
}
